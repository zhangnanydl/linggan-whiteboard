# 参与贡献

感谢你愿意帮助改进灵感白板。无论是问题报告、功能建议、文档修正还是代码提交，都很有价值。

## 提交问题前

1. 搜索现有 Issues，确认没有重复问题。
2. 使用最新版本复现。
3. 记录 Windows 版本、灵感白板版本和具体操作步骤。
4. 如果可能，附上截图或录屏；请先移除学生姓名等敏感信息。

## 本地开发

```powershell
npm install
npm run dev
```

提交代码前至少运行：

```powershell
npm run build
```

涉及桌面行为或打包流程时，还应运行：

```powershell
npm run desktop
.\一键打包.ps1 -SkipInstall -NoOpenFolder
```

## Pull Request 流程

1. Fork 仓库并从默认分支创建功能分支。
2. 每个 PR 只解决一个清晰的问题。
3. 保持现有视觉语言和中文界面文案风格。
4. 不要提交 `node_modules`、临时打包目录或个人配置。
5. 描述改动动机、验证方法和界面变化；UI 改动请附前后截图。
6. 确认没有把真实课堂数据或个人信息提交到仓库。

## 代码约定

- 优先使用清晰、易维护的 React 函数组件。
- 绘图元素应保持可序列化，确保能够写入本地存储。
- 新交互需要考虑鼠标、触控和键盘操作。
- 不使用浏览器原生 `prompt`、`alert` 或 `confirm` 作为正式产品交互。
- 不引入遥测、广告或远程数据服务，除非经过公开讨论并默认关闭。

提交贡献即表示你同意按照项目的 MIT License 发布贡献内容。
